apiVersion: 1

datasources:
  - name: paperone
    type: yesoreyeram-infinity-datasource
    access: proxy
    url: http://paperone:8000
    isDefault: true
    editable: true

    jsonData:
      auth_method: none
